# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/pai_coding?useUnicode=true&characterEncoding=utf8&&useSSL=true&serverTimezone=GMT%2B8"
    username: root
    password: root
# 日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl